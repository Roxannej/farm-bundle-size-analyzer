import { Logger } from './logger.js';
import { ResolvedUserConfig } from '../index.js';
interface CompilerHandlerOptions {
    clear?: boolean;
}
export declare function compilerHandler(callback: () => Promise<void>, config: ResolvedUserConfig, logger: Logger, options?: CompilerHandlerOptions): Promise<void>;
export {};
