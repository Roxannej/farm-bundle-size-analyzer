export function convertErrorMessage(error) {
    let errorMessage = '';
    try {
        errorMessage = JSON.parse(error.message).join('\n');
    }
    catch {
        errorMessage = error.message;
    }
    return errorMessage;
}
//# sourceMappingURL=error.js.map