import { default as koaCors } from '@koa/cors';
export function cors(devSeverContext) {
    const { config } = devSeverContext;
    if (!config.cors)
        return;
    return koaCors(typeof config.cors === 'boolean' ? {} : config.cors);
}
//# sourceMappingURL=cors.js.map