export function hmrPing() {
    return async (ctx, next) => {
        if (ctx.get('accept') === 'text/x-farm-ping') {
            ctx.status = 204;
        }
        else {
            await next();
        }
    };
}
//# sourceMappingURL=hmrPing.js.map