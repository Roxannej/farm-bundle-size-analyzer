import { Middleware } from 'koa';
import { UserConfig } from '../../config/types.js';
import { Logger } from '../../utils/logger.js';
import type { Server } from '../index.js';
export declare function useProxy(options: UserConfig['server'], devSeverContext: Server, logger: Logger): void;
export declare function proxy(devSeverContext: Server): Middleware;
